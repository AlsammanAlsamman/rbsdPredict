% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_extensions.R
\name{simulate_climate_scenarios}
\alias{simulate_climate_scenarios}
\title{Run Climate Scenario Simulation}
\usage{
simulate_climate_scenarios(
  model,
  baseline_data,
  scenario_grid,
  type = c("A", "PDI"),
  summary_fun = mean
)
}
\arguments{
\item{model}{Trained model from train_ml_models() or train_hybrid_model()}

\item{baseline_data}{Data frame containing at least tmax, tmin, and RH}

\item{scenario_grid}{Data frame with columns dtmax, dtmin, dRH}

\item{type}{Prediction target: "A" or "PDI"}

\item{summary_fun}{Function used to summarize scenario predictions}
}
\value{
A list with scenario_summary and scenario_predictions
}
\description{
Generates scenario-level summaries by applying additive changes to
meteorological variables and predicting A or PDI.
}
\examples{
data(rbsd_data)
processed <- preprocess_data(rbsd_data)
ml <- train_ml_models(processed, models = c("lm", "rf"))
grid <- expand.grid(dtmax = c(-1, 0, 1), dtmin = c(-1, 0, 1), dRH = c(-5, 0, 5))
sim <- simulate_climate_scenarios(ml, rbsd_data[, c("tmax", "tmin", "RH")], grid, "PDI")
}
